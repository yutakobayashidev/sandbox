{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "dependencies": {
    "prettier": "^3.0.3"
  },
  "devDependencies": {
    "turbo": "^1.12.3"
  }
}
